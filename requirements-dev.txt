-r requirements.txt
pytest>=8.0
# No lo importa ningún .py: lo necesita `fastapi.testclient.TestClient`, que es
# quien corre los tests de endpoints. Estaba también en requirements.txt con un
# comentario que decía que se usaba para hablarle al cluster — eso lo hace
# `requests`, que no estaba declarado en ningún lado.
httpx>=0.27
# Scripts de build que no corren en producción:
#   build_favicon.py           → compone static/favicon.png
#   build_vertical_datasets.py → genera datasets/*.log desde las fuentes
Pillow>=10.0
openpyxl>=3.1
