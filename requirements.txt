fastapi>=0.110
uvicorn[standard]>=0.29
openai>=1.30
pydantic>=2.6
# SDK oficial de Huawei OBS (S3-compatible pero con manejo nativo de la-south-2,
# mejor diagnóstico de errores específicos de OBS, y firmas correctas).
esdk-obs-python>=3.23
# SDK oficial de Huawei CSS: firma AK/SK (SDK-HMAC-SHA256) para la API de Cluster
# Routes (salida del cluster a MaaS). Import lazy en main.py (no rompe si falta).
huaweicloudsdkcore>=3.1
huaweicloudsdkcss>=3.1
# Cliente HTTP con el que se le habla al cluster CSS (OpenSearch) y a las APIs de
# Huawei: main.py lo importa en 5 lugares. Estaba SIN declarar y la app arrancaba
# igual porque huaweicloudsdkcore lo arrastra — el día que ese SDK lo suelte, el
# contenedor deja de levantar y el error no apunta acá.
requests>=2.31
# Carga el .env al arrancar (main.py, import de nivel de módulo). Mismo caso:
# entraba transitiva por uvicorn[standard].
python-dotenv>=1.0
# Cifrado en reposo del settings file por-usuario (Fernet). Ya venía transitiva;
# la declaramos porque ahora es dependencia directa (ver maas_integrator._fernet).
cryptography>=42
